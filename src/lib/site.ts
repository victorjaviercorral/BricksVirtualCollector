/**
 * URL pública canónica del despliegue. Sin dominio propio en esta etapa (ADR-007): subdominio
 * gratuito de Vercel. Única fuente de verdad para metadatos (Open Graph, robots, sitemap) — si
 * algún día se consolida bajo un dominio propio, se actualiza aquí y no en cada fichero.
 */
export const SITE_URL = "https://bricks-virtual-collector.vercel.app";
